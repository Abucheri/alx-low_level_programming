#include "main.h"

/**
 * main -  entry point into the program
 * tests if the functin prints a positive or
 * negative integer
 *
 * Return: always 0 for success
 */

int main(void)
{
	int i;

	i = 0;

	postitive_or_negative(i);

	return (0);
}
