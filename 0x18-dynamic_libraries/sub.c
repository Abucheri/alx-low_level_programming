#include <stdio.h>

/**
 * sub - returns result of subtraction from two no.s
 *
 * @a: first no.
 * @b: second no.
 *
 * Return: result from subtraction of a and b
 */

int sub(int a, int b)
{
	return (a - b);
}
